// Generated by CoffeeScript 1.9.3
(function() {
  var AttributeTransformer, moment,
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  moment = require('moment');

  AttributeTransformer = (function(superClass) {
    extend(AttributeTransformer, superClass);

    function AttributeTransformer() {
      return AttributeTransformer.__super__.constructor.apply(this, arguments);
    }

    AttributeTransformer.transformValueForAttribute = function(value, attribute) {
      switch (attribute.type) {
        case 'date':
          if (typeof value === 'string') {
            value = new Date(value);
          }
          return moment(value).toDate();
        case 'bool':
        case 'boolean':
          if (typeof value === 'string') {
            value = value.toLowerCase().trim();
          }
          switch (value) {
            case true:
            case 'true':
            case 1:
            case '1':
            case 'on':
            case 'yes':
              return true;
            default:
              return false;
          }
          break;
        case 'decimal':
        case 'double':
        case 'float':
          return parseFloat(value);
        case 'int':
        case 'integer':
          return parseInt(value, 10);
      }
      return value;
    };

    return AttributeTransformer;

  })(Object);

  module.exports = AttributeTransformer;

}).call(this);
